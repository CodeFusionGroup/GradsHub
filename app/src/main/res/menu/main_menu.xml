<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!--
        This holds items that appear in the overflow menu on the action bar (if the app:showAsAction="never" attribute
        is set as indicated) or on the action bar as action buttons (if the app:showAsAction="isRoom" attribute is set
        as indicated if there's space on the action bar)
     -->

    <group android:id="@+id/logout" >
        <item
            android:id="@+id/action_logout"
            android:orderInCategory="100"
            android:title="@string/logout"
            app:showAsAction="never" />
    </group>

</menu>
