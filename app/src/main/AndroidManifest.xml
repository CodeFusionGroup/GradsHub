<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gradshub">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".authentication.AuthenticationActivity" android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".main.MainActivity"
            android:noHistory="false"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".authentication.TestingActivity"></activity>

        <activity android:name=".main.availablegroups.AvailableTestingActivity"></activity>

        <activity android:name=".authentication.FragmentRegisterActivity"></activity>

        <activity android:name=".authentication.ResetActivity"></activity>

        <activity android:name=".main.profile.ProfileTestActivity"></activity>

        <activity android:name=".main.creategroup.CreateGroupActivity"></activity>

        <activity android:name=".main.createpost.CreatePostActivity"></activity>

        <activity android:name=".main.availablegroups.GroupListActivity"></activity>


    </application>

</manifest>