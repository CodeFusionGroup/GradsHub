<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gradshub">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/applogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/applogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".authentication.AuthenticationActivity" android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".main.MainActivity"
            android:noHistory="false"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <receiver android:name=".main.EventNotificationPublisher" />

        <!-- =========================== TEST FILES ============================= -->
        <activity android:name=".authentication.TestingActivity"/>

        <activity android:name=".main.availablegroups.AvailableTestingActivity"/>

        <activity android:name=".authentication.FragmentRegisterActivity"/>

        <activity android:name=".authentication.ResetActivity"/>

        <activity android:name=".main.profile.ProfileTestActivity"/>

        <activity android:name=".main.creategroup.CreateGroupActivity"/>

        <activity android:name=".main.createpost.CreatePostActivity"/>

        <activity android:name=".main.availablegroups.GroupListActivity"/>

        <activity android:name=".main.home.HomeTestActivity"/>

        <activity android:name=".main.postcomments.PostCommentActivity"/>

       <!-- <activity android:name=".main.mygroups.GroupListlistActivity" ></activity> -->

        <activity android:name=".main.home.FeedListActivity"/>
        <!-- ======================================================================== -->

    </application>

</manifest>
