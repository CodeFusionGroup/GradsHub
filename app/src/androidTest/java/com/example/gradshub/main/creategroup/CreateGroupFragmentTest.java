package com.example.gradshub.main.creategroup;

import com.android21buttons.fragmenttestrule.FragmentTestRule;
import com.example.gradshub.R;

import org.junit.Rule;
import org.junit.Test;

import static androidx.test.espresso.Espresso.onView;
import static androidx.test.espresso.assertion.ViewAssertions.matches;
import static androidx.test.espresso.matcher.ViewMatchers.isDisplayed;
import static androidx.test.espresso.matcher.ViewMatchers.withId;
import static org.junit.Assert.*;

public class CreateGroupFragmentTest {

    /*@Rule
    public FragmentTestRule<?,CreateGroupFragment> fragmentTestRule = FragmentTestRule.create(CreateGroupFragment.class);

    @Test
    public void CreateGroupLaunched()
    {
        onView(withId(R.id.groupNameContainer)).check(matches(isDisplayed()));
        onView(withId(R.id.groupVisibilityTV)).check(matches(isDisplayed()));
        onView(withId(R.id.radioGroup)).check(matches(isDisplayed()));
        onView(withId(R.id.doneBtn)).check(matches(isDisplayed()));
    }*/

}